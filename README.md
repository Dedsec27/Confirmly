# Confirmly v50 — cloud startup hardening

This build starts Supabase workspace hydration before UI rendering and isolates every dashboard render block so a missing element cannot prevent cloud persistence.
